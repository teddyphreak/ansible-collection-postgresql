[tool.poetry]
name = "nephelaiio-postgresql"
version = "0.1.0"
description = ""
authors = ["Ted Cook <teodoro.cook@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
ansible-lint = { version = "^6.20.4", markers = "platform_system != 'Windows'" }
ansible = "^8.7.0"
netaddr = "^0.9.0"
yamllint = "^1.32.0"
molecule = "^6.0.3"
molecule-plugins = {extras = ["docker"], version = "^23.5.3"}
lxml = "^5.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
